<div id="top"><?= $this->partial('layout/admin_top_menu', array('tittle' => 'Delete Category')); ?></div>
<div id="left"> <?= $this->partial('layout/left-menu', array('faq' => 'active')); ?></div> 
<div id="content"> 
    <div class="outer">
        <div class="inner">

            <p>Are you sure that you want to delete
                '<?php echo $this->escapeHtml($this->faq->question); ?>'?
            </p>
            <?php
            $url = $this->url('admin/faq', array(
                'action' => 'delete',
                'id' => $this->faq->id,
            ));
            ?>
            <form action="<?php echo $url; ?>" method="post">
                <div>
                    <input type="hidden" name="id" value="<?php echo (int) $this->faq->id; ?>" />
                    <input type="submit" name="del" value="Yes" />
                    <input type="submit" name="del" value="No" />
                </div>
            </form>
        </div>
    </div>
</div>
