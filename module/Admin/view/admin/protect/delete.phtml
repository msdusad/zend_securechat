<div id="top"><?= $this->partial('layout/admin_top_menu', array('tittle' => 'Delete Protect')); ?></div>
<div id="left"> <?= $this->partial('layout/left-menu', array('protect' => 'active')); ?></div> 
<div id="content"> 
    <div class="outer">
        <div class="inner">

            <p>Are you sure that you want to delete
                '<?php echo $this->escapeHtml($this->protect->title); ?>'?
            </p>
            <?php
            $url = $this->url('admin/protect', array(
                'action' => 'delete',
                'id' => $this->protect->id,
            ));
            ?>
            <form action="<?php echo $url; ?>" method="post">
                <div>
                    <input type="hidden" name="id" value="<?php echo (int) $this->protect->id; ?>" />
                    <input type="submit" name="del" value="Yes" />
                    <input type="submit" name="del" value="No" />
                </div>
            </form>
        </div>
    </div>
</div>
